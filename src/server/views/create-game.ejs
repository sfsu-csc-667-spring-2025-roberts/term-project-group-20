<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Game - Uno</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }
    
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: #f5f5f5;
    }
    
    .container {
      width: 100%;
      max-width: 1000px;
      height: 600px;
      border: 2px solid #333;
      background-color: #fff;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      position: relative;
    }
    
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 20px;
      border-bottom: 2px solid #333;
    }
    
    .title {
      text-align: center;
      font-size: 32px;
      font-weight: bold;
      position: absolute;
      width: 100%;
      top: 20px;
      left: 0;
      z-index: 1;
    }
    
    .logo {
      font-size: 28px;
      font-weight: bold;
      border: 2px solid #333;
      border-radius: 50%;
      padding: 5px 10px;
      z-index: 2;
    }
    
    .nav-buttons {
      display: flex;
      gap: 10px;
      z-index: 2;
    }
    
    .btn {
      padding: 8px 15px;
      border: 2px solid #333;
      border-radius: 20px;
      background-color: #fff;
      font-size: 14px;
      cursor: pointer;
      text-decoration: none;
      color: #000;
    }
    .logo {
    height: 50px; 
    margin-right: 15px;
    }
    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 40px;
    }
    
    .create-form {
      width: 100%;
      max-width: 500px;
    }
    
    .form-group {
      margin-bottom: 30px;
      display: flex;
      align-items: center;
    }
    
    .form-group label {
      width: 200px;
      font-size: 18px;
      margin-right: 15px;
    }
    
    .form-group input {
      flex: 1;
      padding: 10px;
      border: 2px solid #333;
      border-radius: 5px;
      font-size: 16px;
    }
    
    .start-btn {
      display: block;
      width: 200px;
      padding: 12px;
      border: 2px solid #333;
      border-radius: 20px;
      background-color: #fff;
      font-size: 18px;
      cursor: pointer;
      text-align: center;
      margin: 30px auto 0;
    }
    
    .error {
      color: #e74c3c;
      text-align: center;
      margin-bottom: 20px;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="https://upload.wikimedia.org/wikipedia/commons/f/f9/UNO_Logo.svg" alt="UNO" class="logo">
      <h1 class="title">Create Game</h1>
      <div class="nav-buttons">
        <a href="/lobby" class="btn">Back to lobby</a>
        <a href="/auth/login" class="btn">Log out</a>
      </div>
    </div>
    
    <div class="content">
      <% if (locals.error && error) { %>
        <p class="error"><%= error %></p>
      <% } %>

      <%# Game Creation %>
      <form class="create-form" action="/games/create" method="POST">
        <div class="form-group">
        
          <label for="gameName">Game Name</label>
          <input 
            type="text" 
            id="gameName" 
            name="gameName" 
            <% if (locals.formData && formData.gameName) { %>
              value="<%= formData.gameName %>"
            <% } %>
            required
          >
        </div>
        
        <div class="form-group">
          <label for="maxPlayers">Max # of Players</label>
          <input 
            type="number" 
            id="maxPlayers" 
            name="maxPlayers" 
            min="2" 
            max="10" 
            <% if (locals.formData && formData.maxPlayers) { %>
              value="<%= formData.maxPlayers %>"
            <% } else { %>
              value="4"
            <% } %>
            required
          >
        </div>
        
        <button type="submit" class="start-btn">Start Game</button>
      </form>
    </div>
  </div>
</body>
</html>