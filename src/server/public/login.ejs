<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Log in - Uno</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }
    
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: #f5f5f5;
    }
    
    .container {
      width: 100%;
      max-width: 1000px;
      height: 600px;
      border: 2px solid #333;
      background-color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    
    .title {
      text-align: center;
      margin-bottom: 30px;
      font-size: 32px;
      font-weight: bold;
    }
    
    .login-card {
      width: 400px;
      padding: 30px;
      border: 2px solid #333;
      border-radius: 10px;
      background-color: #f9f9f9;
    }
    
    .card-title {
      text-align: center;
      margin-bottom: 25px;
      font-size: 24px;
    }
    
    .form-group {
      margin-bottom: 20px;
    }
    
    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 10px;
      border: 2px solid #333;
      border-radius: 20px;
      font-size: 16px;
    }
    
    .btn {
      width: 100%;
      padding: 10px;
      border: 2px solid #333;
      border-radius: 20px;
      background-color: #fff;
      font-size: 16px;
      cursor: pointer;
      text-align: center;
      margin-bottom: 15px;
    }
    
    .divider {
      text-align: center;
      margin-bottom: 15px;
      font-weight: bold;
    }
    
    .register-link {
      display: block;
      width: 120px;
      padding: 10px;
      border: 2px solid #333;
      border-radius: 20px;
      background-color: #fff;
      font-size: 16px;
      cursor: pointer;
      text-align: center;
      margin: 0 auto;
      text-decoration: none;
      color: #000;
    }
    
    .error {
      color: #e74c3c;
      text-align: center;
      margin-bottom: 15px;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="login-content">
      <h1 class="title">Log in</h1>
      
      <div class="login-card">
        <h2 class="card-title">Login to Play</h2>
        
        <% if (locals.error && error) { %>
          <p class="error"><%= error %></p>
        <% } %>
        
        <form class="auth-form" action="/auth/login" method="POST">
          <div class="form-group">
            <input 
              type="email" 
              id="email" 
              name="email" 
              placeholder="Email address"
              <% if(locals.email && email) { %>
                value="<%= email %>"
              <% } %>
              required
            >
          </div>
          
          <div class="form-group">
            <input 
              type="password" 
              id="password" 
              name="password" 
              placeholder="Password"
              required
            >
          </div>
          
          <button type="submit" class="btn">Login</button>
          
          <div class="divider">OR</div>
          
          <a href="/auth/register" class="register-link">Sign up</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
